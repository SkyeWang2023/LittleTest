void read_buffer(char* buf, int offset) {
    char* ptr = buf + offset;  
    char c = *ptr;  
}

bool copy_name(char *dst, size_t dst_size, const char *src)
{
  strcpy(dst, src);
  return true;
}

void test()
{
  int x;
  int y = x;
}
